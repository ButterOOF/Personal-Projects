Server that is has a target number that is randomized and a cilent that gets to send packets containing their guess with the server sending back a packet if it is right or wrong. Repeats until the cilent sends a packet containing the target number.
